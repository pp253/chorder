/**
 * 純 = (可以省略)
 * 大 +
 * 小 -
 * 增 ++
 * 減 --
 * 倍增 +++
 * 倍減 ---
 * 例如小三度就是 `-3`，大三度是 `+3`。而純五度可以寫 `=5`，或直接寫成 `5`。
 */

const basicChords = {
  'X': ['1', '+3', '5'],
  'Xm': ['1', '-3', '5']
}

const chords = {

}
